{
  "name": "ove-vscode-primer-cart",
  "displayName": "Open Vector Editor + Primer Cart",
  "description": "View plasmid sequences in VS Code and collect primers across files into one orderable list",
  "version": "1.5.1",
  "publisher": "gordonrix",
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/gordonrix/ove-vscode"
  },
  "engines": {
    "vscode": "^1.98.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "DNA",
    "vector",
    "genbank",
    "biology",
    "plasmid",
    "research",
    "primer",
    "oligo"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./extension.js",
  "contributes": {
    "languages": [
      {
        "id": "dna",
        "extensions": [
          ".dna",
          ".fasta",
          ".fa",
          ".gbk",
          ".gb"
        ],
        "aliases": [
          "DNA",
          "dna"
        ]
      }
    ],
    "customEditors": [
      {
        "viewType": "oveCart.editor",
        "displayName": "OVE",
        "selector": [
          {
            "filenamePattern": "*.dna",
            "binary": true
          },
          {
            "filenamePattern": "*.fasta"
          },
          {
            "filenamePattern": "*.fa"
          },
          {
            "filenamePattern": "*.gb"
          },
          {
            "filenamePattern": "*.gbk"
          }
        ]
      }
    ],
    "commands": [
      {
        "command": "oveCart.showEditor",
        "title": "Open Vector Editor: Open Demo Editor"
      },
      {
        "command": "oveCart.show",
        "title": "Primer Cart: Show Cart"
      },
      {
        "command": "oveCart.copyTsv",
        "title": "Primer Cart: Copy All as TSV"
      },
      {
        "command": "oveCart.copySequences",
        "title": "Primer Cart: Copy All Sequences"
      },
      {
        "command": "oveCart.exportCsv",
        "title": "Primer Cart: Export CSV..."
      },
      {
        "command": "oveCart.newSession",
        "title": "Primer Cart: New Session"
      },
      {
        "command": "oveCart.manageSessions",
        "title": "Primer Cart: Switch or Manage Sessions"
      },
      {
        "command": "oveCart.refreshInventory",
        "title": "Primer Cart: Reload Primer Inventory"
      },
      {
        "command": "oveCart.clear",
        "title": "Primer Cart: Clear Current Session"
      },
      {
        "command": "oveCart.pickInventoryFile",
        "title": "Primer Cart: Choose Primer Inventory File..."
      }
    ],
    "configuration": {
      "title": "Open Vector Editor + Primer Cart",
      "properties": {
        "oveCart.viewType": {
          "type": "string",
          "default": "sequence",
          "description": "Default view type when a sequence is opened.",
          "enum": [
            "sequence",
            "circular",
            "split"
          ],
          "enumDescriptions": [
            "Show sequence only.",
            "Show circular map only.",
            "Show sequence and circular map."
          ]
        },
        "oveCart.readOnly": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Open sequences with editing locked. While locked, the **Create** menu (New Feature, New Primer, ...) is hidden entirely. You can always toggle the padlock in the editor toolbar for a single file."
        },
        "oveCart.allowSequenceEditing": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Allow the DNA bases themselves to be edited. When off (the default), annotations can be created and edited but the sequence cannot be cut, pasted over, complemented or deleted."
        },
        "oveCart.autoAddCreatedPrimers": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Automatically add a primer to the Primer Cart when you create one with **Create -> New Primer**."
        },
        "oveCart.maxItems": {
          "type": "number",
          "default": 500,
          "minimum": 1,
          "description": "Maximum number of primers the cart will hold. Adds beyond this are refused rather than silently dropping older entries."
        },
        "oveCart.inventoryPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to a primer inventory `.xlsx` or `.csv`, used to flag cart primers that you have already ordered. Supports `~` and `${workspaceFolder}`. Leave empty to disable cross-referencing."
        },
        "oveCart.inventorySheet": {
          "type": "string",
          "default": "",
          "description": "Worksheet name to read from an .xlsx inventory. Leave empty to use the first sheet."
        },
        "oveCart.inventoryNameColumn": {
          "type": "string",
          "default": "",
          "markdownDescription": "Header of the column holding primer names or IDs. Leave empty to use the first column. The header must match the file exactly, including any special characters such as `°` or `μ`; if it does not match, the error message lists the headers actually found."
        },
        "oveCart.inventorySequenceColumn": {
          "type": "string",
          "default": "",
          "markdownDescription": "Header of the column holding primer sequences. Leave empty to use the second column. Matching against the cart is by exact sequence, ignoring case and whitespace."
        },
        "oveCart.sequenceCopyIncludesName": {
          "type": "string",
          "default": "sequence",
          "description": "Format used by the \"Copy sequences\" button.",
          "enum": [
            "sequence",
            "name-tab-sequence"
          ],
          "enumDescriptions": [
            "One bare sequence per line.",
            "Name and sequence separated by a tab, one per line."
          ]
        },
        "oveCart.searchMinAnneal": {
          "type": "number",
          "default": 15,
          "minimum": 8,
          "markdownDescription": "Minimum number of bases at a primer's 3' end that must match the template exactly for it to count as a hit. Lower finds more and noisier; higher is stricter."
        },
        "oveCart.searchFullLengthOnly": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Start each primer search with **100% match only** ticked, hiding primers whose 5' tail is absent from the template."
        },
        "oveCart.searchMaxHits": {
          "type": "number",
          "default": 500,
          "minimum": 1,
          "description": "Safety cap on how many primer-search hits are returned. Overflow is reported in the results header."
        },
        "oveCart.inventoryAliasColumn": {
          "type": "string",
          "default": "",
          "markdownDescription": "Header of an optional column shown alongside each search hit. Leave empty to use a column named `Alias` if one exists; a missing column is never an error."
        },
        "oveCart.inventoryDescriptionColumn": {
          "type": "string",
          "default": "",
          "markdownDescription": "Header of an optional description column shown as a tooltip on search hits. Leave empty to use a column named `Description` if one exists."
        }
      }
    }
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "yarn run lint",
    "test": "node --test 'test/unit/*.test.js'",
    "test:vscode": "vscode-test",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.98.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "@vscode/vsce": "^3.2.1",
    "depcheck": "^1.4.7",
    "eslint": "^9.21.0",
    "globals": "^15.14.0"
  }
}
